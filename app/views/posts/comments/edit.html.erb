<%= turbo_frame_tag dom_id @comment do %>
  <%= render 'form', post: @comment.post, comment: @comment %>
  <div class="mt-3">
    <%= link_to 'キャンセル', post_path(@comment.post), class: 'btn btn-link btn-sm' %>
  </div>
  <hr>
<% end %>